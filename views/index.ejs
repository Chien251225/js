
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tree Shop</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body>
    <header class="mb-4">
        <h1 class="text-center">Tree Shop</h1>
        <nav class="text-center">
            <a href="/" class="btn btn-outline-light">Home</a>
            <a href="/about" class="btn btn-outline-light">About</a>
        </nav>
    </header>
    
    <div class="container">
        <h2 class="text-center text-success">Tree Shop</h2>
        <form action="/add" method="POST" class="mb-4">
            <div class="mb-3">
                <label class="form-label">Tree Name</label>
                <input type="text" name="treename" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Description</label>
                <textarea name="description" class="form-control" required></textarea>
            </div>

            <div class="mb-3">
                <label class="form-label">Image URL</label>
                <input type="text" name="image" class="form-control">
            </div>

            <button type="submit" class="btn btn-success">Add</button>
            <button type="submit" formaction="/reset" class="btn btn-secondary">Reset</button>
        </form>

        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Image</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% trees.forEach((tree, index) => { %>
                    <tr>
                        <td class="text-center"><%= index + 1 %></td>
                        <td><%= tree.treename %></td>
                        <td class="text-center">
                            <% if (tree.image) { %>
                                <img src="<%= tree.image %>" class="img-thumbnail">
                            <% } else { %>
                                <span class="text-muted">No image</span>
                            <% } %>
                        </td>
                        <td><%= tree.description %></td>
                        <td class="text-center">
                            <form action="/delete/<%= tree._id %>" method="POST">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

    <footer>
        <p>Số 8, Tôn Thất Thuyết, Cầu Giấy, Hà Nội</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
